spring:
  application:
    name: consumer
server:
  port: 90
eureka:
  client:
    service-url:
      defaultZone: http://registrar:7900/eureka/
  instance:
    metadata-map:
      dalao: lisz-hahaha
